# Public (safe) build-time config for the frontend
# Used by Vite during `npm run build`
#
# If you deploy the backend on the same origin (e.g. Netlify/Vercel), leave this empty
# and the frontend will call /api/... on the same domain.
VITE_API_BASE=
