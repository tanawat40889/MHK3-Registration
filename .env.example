# Example local environment file (DO NOT put real secrets in committed files)
#
# 1) Copy to .env
#    cp .env.example .env
# 2) Fill NOTION_TOKEN
# 3) Run local API server
#    npm run api
#
# Notion internal integration token (server-side only)
NOTION_TOKEN=""

# Port for the local API server
PORT=8787

# Optional: build-time frontend API base
# For production (Neocities + Cloudflare Worker), this is set in .env.production
# VITE_API_BASE=https://bkk11-notion-proxy.bkk11.workers.dev
